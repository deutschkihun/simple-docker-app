language: generic

services:
  - docker

os: linux

dist: xenial

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t deutschkihun/simple-docker-react-app -f Dockerfile.dev .

script:
  - docker run -e CI=true deutschkihun/simple-docker-react-app npm run test -- --coverage

after_success:
  - echo "Test success"

after_failure:
  - echo "Test Fail!"
